"use client";
import Image from "next/image";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { brandAssets, navLinks, site } from "@/lib/site";

function isActivePath(pathname:string,href:string){if(href==="/")return pathname==="/";return pathname.startsWith(href)}
export function Header(){const pathname=usePathname();return <header className="sticky top-0 z-50 px-3 pt-3 sm:px-4 lg:px-6"><div className="mx-auto max-w-7xl"><div className="glass-surface flex min-h-[4.75rem] items-center justify-between gap-4 rounded-[1.5rem] px-4 sm:px-5"><Link className="flex min-w-0 items-center gap-3" href="/"><span className="grid h-12 w-12 shrink-0 place-items-center overflow-hidden rounded-xl bg-white p-1 shadow-sm"><Image src={brandAssets.logo.src} alt="" width={44} height={44} className="h-full w-full object-contain" /></span><span className="min-w-0"><span className="block truncate font-display text-xl font-bold uppercase leading-none tracking-[0.04em] text-brand-navy sm:text-2xl">{site.name}</span><span className="mt-1 hidden text-[0.65rem] font-bold uppercase tracking-[0.2em] text-slate-500 sm:block">Local plumbing services</span></span></Link><nav className="hidden items-center gap-1 lg:flex">{navLinks.map(link=><Link key={link.href} className={`rounded-full px-3.5 py-2 text-sm font-bold transition ${isActivePath(pathname,link.href)?"bg-white text-brand-blue shadow-sm":"text-slate-700 hover:bg-white/80 hover:text-brand-blue"}`} href={link.href}>{link.label}</Link>)}</nav><Link className="inline-flex shrink-0 items-center justify-center rounded-full bg-brand-blue px-4 py-2.5 text-sm font-black text-white shadow-lift transition hover:-translate-y-0.5 hover:bg-blue-700" href="/contact">Request Service</Link></div></div></header>}
